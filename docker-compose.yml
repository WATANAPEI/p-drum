version: "3.7"
services:
  p-build:
      build: .
      container_name: "p-build"
      volumes:
          - type: volume
            source: out
            target: /home/app/p-drum/out
      networks:
          - mynet

volumes:
    out:
        external: true

networks:
    mynet:
        external: true
